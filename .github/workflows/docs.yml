name: Deploy Docs

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  disabled:
    name: Docs deployment disabled
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Notice
        run: echo "Docs deployment is disabled. Documentation is maintained outside this pipeline."
